- [x] Изучить текущий код Telegram Login в `web/scripts/main.js` и понять формат данных, которые отправляются на `/api/v1/auth/telegram`.
- [x] Изменить фронтенд: нормализовать payload Telegram Login перед отправкой (извлекать `auth_data`/`authData` при необходимости).
- [x] Добавить fallback-ошибку, если payload распознать не удалось.
- [x] Протестировать вход через Telegram (обе ветки: когда iframe присылает `auth_result`/`auth_user` и когда присылает обёртку).
- [x] Исправить бэкенд: верификация Telegram-подписки по всем полям, а не по хардкод-списку.

